name: helm
description: "Helm dashboard — captain/firstmate/crew task board for agentic development."
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7
  dio: ^5.7.0
  go_router: ^14.6.0
  web_socket_channel: ^3.0.0
  flutter_markdown: ^0.7.4
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
